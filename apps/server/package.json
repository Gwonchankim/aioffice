{
  "name": "@orion/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "default": "./dist/main.js"
    }
  },
  "scripts": {
    "start": "node dist/main.js",
    "typecheck": "pnpm --filter @orion/contracts build && pnpm --filter @orion/orchestration build && pnpm --filter @orion/agent-catalog build && tsc --project tsconfig.json --noEmit",
    "test": "pnpm --filter @orion/contracts build && pnpm --filter @orion/orchestration build && pnpm --filter @orion/agent-catalog build && vitest run --config ../../vitest.config.ts",
    "test:coverage": "pnpm --filter @orion/contracts build && pnpm --filter @orion/orchestration build && pnpm --filter @orion/agent-catalog build && vitest run --coverage --config ../../vitest.config.ts",
    "build": "pnpm --filter @orion/contracts build && pnpm --filter @orion/orchestration build && pnpm --filter @orion/agent-catalog build && tsc --project tsconfig.json"
  },
  "dependencies": {
    "@orion/contracts": "workspace:*",
    "@orion/orchestration": "workspace:*",
    "@orion/agent-catalog": "workspace:*",
    "fastify": "^5.3.2",
    "pino": "^9.7.0",
    "ulid": "^2.3.0",
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@orion/test-fixtures": "workspace:*"
  }
}
