# CORR-M2-SMOKE-CONFIG-003 — Decision Log

- **WORKFLOW_MODE = controller_isolated.** P2 review (3 rounds) and P4 validation ran in distinct isolated worker contexts with recorded worker IDs (see worker-registry.md).
- **CFG-001 schema is a strict cross-provider subset WITHOUT `maxItems`.** Round-1 review corrected a blocker: Anthropic structured outputs support only array `minItems` (0/1) and reject `maxItems`; `maxItems:0` would make Claude reject the schema. Decision: one shared schema with real `items` + `additionalProperties:false` + `required`=all-properties, and enforce emptiness+success smoke-locally via `smokeResultIsStrict` (Zod `runResultSchema` pass AND status succeeded AND all five arrays empty). This is authoritative for `strictResult`.
- **CFG-002/RB5 prompt is provider-neutral.** The shared prompt names no provider-specific tools (Read/Glob/Grep are Claude-only; Codex inspects under `--sandbox read-only`). Read-only enforcement stays in each provider's argv.
- **CFG-003 official empty MCP config.** Claude `--mcp-config` receives `{"mcpServers":{}}` (JSON-parses to exactly `{mcpServers:{}}`), keeping `--strict-mcp-config`; the bare `'{}'` is removed. Connector/account info is never in evidence.
- **CFG-004 sanitized diagnostics.** A closed enum + a frozen priority-ordered pattern table; input bounded to 16 KiB; first-match wins; unmatched non-empty → `UNKNOWN_PROVIDER_FAILURE`; empty → undefined. Only codes are stored (evidence + outcome ledger); raw stderr/stdout/message/path/identity is discarded (a transient bounded buffer is classified then GC'd).
- **CFG-005/006 terminal failure gates success.** Codex `error`/`turn.failed` and Claude error-result frames set `terminalFailure`; final `strictResult = hasStrictResult && !terminalFailure`, so a valid result on exit 0 accompanied by a terminal-failure frame is NOT a success. A Claude pre-frame nonzero exit keeps 0 events + a stderr diagnostic + no retry. The shared normalizer is unchanged (handling is smoke-local).
- **CFG-007 additive evidence correction.** The prior "external-state-only" framing is superseded: two real smoke config/code defects (missing array `items`; non-official MCP config) are the corrected, grounded hypotheses (not confirmed certainties without a new authorized smoke). `gpt-5.6-sol` is a supported model, NOT invalid/fictional. Prior evidence + the spent ledger are preserved unchanged.
- **CFG-008 policy v3.** `ARGV_POLICY_VERSION` 2→3; the schema/prompt hashes change; `computeLivePolicy()` therefore differs, so any prior grant (incl. the spent `M2-SMOKE-20260724-001`) fails `policy_binding_mismatch` (0 spawn) by construction — proven by a frozen generation-2 regression test with zero ledger mutation.
- **NOT done (require separate explicit authorization).** Real provider smoke; new grant; new authorization id; main integration; M3. No `ORION_REAL_PROVIDER_TESTS`; no authorization reuse; the existing ledger/evidence are read-only.
