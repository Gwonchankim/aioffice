# M2-REAL-PROVIDER-SMOKE-002 — Decision Log

- **WORKFLOW_MODE = controller_isolated.** The single real smoke ran in one isolated executor (`15-M2-Real-Smoke-002-Run`, distinct context from the controller). No same-session role switch substituted for the isolated executor; no new worker was started after the smoke began.
- **Grant + verification done in the controller (0 real model calls); the real smoke delegated to one isolated executor.** Rationale: the grant makes zero real provider/model calls (it resolves executables, computes SHA-256 fingerprints, runs `--version`, and writes the ledger). Performing the grant + its verification in the controller means the §4 GRANT_BLOCKED safety gate is evaluated with full rigor BEFORE any real call is authorized; the real-call work (§5) is then isolated in exactly one executor invocation, satisfying §2.3's core intent (real smoke isolated, not fragmented, not controller-run).
- **Model binding.** The run sourced its model solely from the persisted grant (no `ORION_CODEX_SMOKE_MODEL`/`ORION_CLAUDE_SMOKE_MODEL` passed to the run command). Authorized models: Codex `gpt-5.6-sol`, Claude `sonnet` (underlying `claude-sonnet-4-6`). No substitute, retry-with-other-model, or fallback.
- **Environment scoping.** All real-provider env vars were passed per-command via the tool's scoped `env` parameter (grant: authId+executables+models; run: authId+executables+`ORION_REAL_PROVIDER_TESTS=1`, no model vars), so nothing persisted and no cleanup-failure risk existed. Post-run `env | grep ORION_` ⇒ NONE.
- **Authoritative invocation count = ledger `.spawn` markers.** New ledger has exactly 2 (`openai-1.spawn`, `anthropic-1.spawn`); each provider ≤1; total ≤2. No over-invocation.
- **Result = PASS.** Both providers `succeeded` + `strictResult:true` + `repositoryUnchanged:true` + `sanitizerFindingCount:0` + descendant leak 0; `cleanup:complete`. The corrected harness (CORR-M2-SMOKE-CONFIG-003: real schema array `items` + official `{"mcpServers":{}}` MCP config) resolved the prior `M2-REAL-PROVIDER-SMOKE-001` failures. `gpt-5.6-sol` is confirmed a working, supported Codex model here.
- **NOT done.** main integration (a SEPARATE explicit decision) was not performed. No recall/retry/fallback. No product/dependency/lockfile/schema/prompt change. No push/PR/deploy/external action. The spent `M2-SMOKE-20260724-001` ledger and prior evidence are preserved unchanged. M3 not started.
- **Next decision = user.** Proceed (or not) to main-integration review of the M2 correction chain.
